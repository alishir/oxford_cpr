[{kernel,
  [{distributed, [{m8ball,
                   5000,
                  [a@localhost, {b@localhost, c@localhost}]}]},
   {sync_nodes_optional, [a@localhost, c@localhost]},
   {sync_nodes_timeout, 30000}
  ]
 }
].
